Kind: ClusterConfig
apiVersion: eksctl.io/v1alpha5

metadata:
  name: expesne
  region: us-east-1
  
managedNodeGroups:
- name: spot 
  minSize: 1 
  maxSize: 3 
  instanceType: m5.large
  desiredCapacity: 2
  spot: true